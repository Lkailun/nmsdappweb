import { FC, ReactElement } from 'react';

import { Header, Section } from './components';
const Home: FC = (): ReactElement => {
    return (
        <>
            <Header />
            <Section />
        </>
    );
};

export default Home;
